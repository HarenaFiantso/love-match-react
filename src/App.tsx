import { LoveForm, AppBackground } from './components';

export default function App() {
  return (
    <main className="w-full h-screen flex items-center justify-center">
      <AppBackground />
      <LoveForm />
    </main>
  );
}
